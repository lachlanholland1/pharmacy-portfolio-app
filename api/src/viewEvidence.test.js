let viewEvidence = require("../pharmacy_routes/ViewEvidence");
const express = require("express");
const router = express.Router();


it("Testing to see if Jest works", () => {
  expect(1).toBe(1);
});